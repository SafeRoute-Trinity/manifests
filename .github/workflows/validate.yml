name: Validate Kubernetes Manifests

on:
  pull_request:
    paths:
      - 'k8s/**'
      - '.github/workflows/**'
  push:
    branches:
      - main
    paths:
      - 'k8s/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'v1.28.0'

      - name: Install yq for YAML processing
        run: |
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64 || \
            curl -L https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64 -o /usr/local/bin/yq
          sudo chmod +x /usr/local/bin/yq
          yq --version || echo "⚠️ yq installation failed"

      - name: Validate YAML syntax
        run: |
          echo "🔍 Validating YAML syntax..."
          errors=0
          
          for file in $(find k8s -name "*.yml" -o -name "*.yaml"); do
            if command -v yq >/dev/null 2>&1; then
              if ! yq eval '.' "$file" >/dev/null 2>&1; then
                echo "❌ Invalid YAML syntax in $file"
                errors=$((errors + 1))
              fi
            else
              # Fallback: basic YAML check
              if ! python3 -c "import yaml; yaml.safe_load(open('$file'))" 2>/dev/null; then
                echo "❌ Invalid YAML syntax in $file"
                errors=$((errors + 1))
              fi
            fi
          done
          
          if [ $errors -gt 0 ]; then
            echo "❌ Found $errors YAML syntax errors"
            exit 1
          fi
          echo "✅ All YAML files have valid syntax"

      - name: Check for StorageClass resources
        run: |
          echo "🔍 Checking for StorageClass resources..."
          storageclass_files=""
          
          for file in $(find k8s -name "*.yml" -o -name "*.yaml"); do
            if grep -qE "^\s*kind:\s*StorageClass\s*$" "$file"; then
              storageclass_files="${storageclass_files}\n  - $file"
            fi
          done
          
          if [ -n "$storageclass_files" ]; then
            echo "❌ ERROR: StorageClass resources found in manifests:"
            echo -e "$storageclass_files"
            echo ""
            echo "StorageClasses are managed by AKS and should not be in manifests."
            echo "Please remove these StorageClass definitions."
            exit 1
          fi
          echo "✅ No StorageClass resources found"

      - name: Validate Kubernetes resource schemas
        run: |
          echo "🔍 Validating Kubernetes resource schemas..."
          errors=0
          
          # Function to check if file is a Kubernetes manifest
          is_k8s_manifest() {
            local file=$1
            # Skip values.yaml files (Helm values, not K8s manifests)
            if echo "$file" | grep -qE "values\.ya?ml$"; then
              return 1
            fi
            # Check if file contains a kind field (Kubernetes resource)
            if grep -qE "^\s*kind:\s*" "$file"; then
              return 0
            fi
            return 1
          }
          
          # Function to validate a manifest file using yq (no cluster needed)
          validate_manifest() {
            local file=$1
            
            # Skip non-Kubernetes files
            if ! is_k8s_manifest "$file"; then
              echo "⏭️  Skipping non-Kubernetes file: $file"
              return 0
            fi
            
            if ! command -v yq >/dev/null 2>&1; then
              echo "⚠️  yq not available, skipping detailed validation for $file"
              return 0
            fi
            
            # Extract kind and name using yq
            local kind=$(yq eval '.kind' "$file" 2>/dev/null | tr -d '"' | tr -d "'")
            local name=$(yq eval '.metadata.name // ""' "$file" 2>/dev/null | tr -d '"' | tr -d "'")
            local namespace=$(yq eval '.metadata.namespace // ""' "$file" 2>/dev/null | tr -d '"' | tr -d "'")
            
            if [ -z "$kind" ]; then
              echo "⚠️  Warning: Could not determine kind in $file"
              return 0
            fi
            
            # Validate required fields exist
            local has_api_version=$(yq eval '.apiVersion // ""' "$file" 2>/dev/null)
            local has_metadata=$(yq eval '.metadata // ""' "$file" 2>/dev/null)
            
            if [ -z "$has_api_version" ]; then
              echo "❌ $kind/$name in $file: missing apiVersion"
              return 1
            fi
            
            if [ -z "$has_metadata" ]; then
              echo "❌ $kind/$name in $file: missing metadata"
              return 1
            fi
            
            if [ -z "$name" ]; then
              echo "❌ $kind in $file: missing metadata.name"
              return 1
            fi
            
            # Try kubectl validation if possible (but don't fail if it can't connect)
            # Create a dummy kubeconfig to prevent connection attempts
            local dummy_kubeconfig=$(mktemp)
            echo "apiVersion: v1" > "$dummy_kubeconfig"
            echo "kind: Config" >> "$dummy_kubeconfig"
            echo "clusters: []" >> "$dummy_kubeconfig"
            echo "users: []" >> "$dummy_kubeconfig"
            echo "contexts: []" >> "$dummy_kubeconfig"
            
            KUBECONFIG="$dummy_kubeconfig" kubectl apply --dry-run=client -f "$file" >/dev/null 2>&1
            local kubectl_exit=$?
            rm -f "$dummy_kubeconfig"
            
            if [ $kubectl_exit -eq 0 ]; then
              if [ -n "$namespace" ]; then
                echo "✅ $kind/$name in namespace $namespace ($file)"
              else
                echo "✅ $kind/$name ($file)"
              fi
            else
              # kubectl validation failed, but we already validated structure with yq
              # This is acceptable - kubectl might need cluster connection for some validations
              if [ -n "$namespace" ]; then
                echo "✅ $kind/$name in namespace $namespace ($file) - structure validated"
              else
                echo "✅ $kind/$name ($file) - structure validated"
              fi
            fi
            
            return 0
          }
          
          # Validate all manifests
          for file in $(find k8s -name "*.yml" -o -name "*.yaml" | sort); do
            if ! validate_manifest "$file"; then
              errors=$((errors + 1))
            fi
          done
          
          if [ $errors -gt 0 ]; then
            echo "❌ Found $errors validation errors"
            exit 1
          fi
          echo "✅ All manifests validated successfully"

      - name: Check for immutable field changes
        run: |
          echo "🔍 Checking for potential immutability issues..."
          warnings=0
          
          # Check PVCs for storageClassName changes
          for file in $(find k8s -name "*pvc*.yml" -o -name "*pvc*.yaml"); do
            if [ -f "$file" ]; then
              echo "📋 Checking PVC in $file"
              if grep -q "storageClassName" "$file"; then
                echo "  ℹ️  PVC has storageClassName defined (immutable after creation)"
                echo "  ⚠️  If this PVC already exists, changing storageClassName will fail"
              fi
            fi
          done
          
          # Check StatefulSets for volumeClaimTemplates
          for file in $(find k8s -name "*statefulset*.yml" -o -name "*statefulset*.yaml"); do
            if [ -f "$file" ]; then
              echo "📋 Checking StatefulSet in $file"
              if grep -q "volumeClaimTemplates" "$file"; then
                echo "  ℹ️  StatefulSet has volumeClaimTemplates (immutable after creation)"
                echo "  ⚠️  If this StatefulSet already exists, changing volumeClaimTemplates will fail"
              fi
            fi
          done
          
          echo "✅ Immutability check complete"

      - name: Validate deployment workflow consistency
        run: |
          echo "🔍 Validating deployment workflow consistency..."
          
          # Check that all service directories referenced in deploy.yml exist
          services=("user-management" "notification-service" "routing-service" "safety-scoring" "sos" "feedback")
          
          for service in "${services[@]}"; do
            service_dir="k8s/saferoute/${service}"
            if [ ! -d "$service_dir" ]; then
              echo "⚠️  Warning: Service directory $service_dir not found but referenced in deploy.yml"
            elif [ ! -f "${service_dir}/deployment.yml" ]; then
              echo "⚠️  Warning: deployment.yml not found in $service_dir"
            fi
          done
          
          echo "✅ Workflow consistency check complete"

      - name: Summary
        if: always()
        run: |
          echo "## ✅ Manifest Validation Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "All Kubernetes manifests have been validated for:" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ YAML syntax" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ No StorageClass resources" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Kubernetes resource schemas" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Immutability warnings" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Workflow consistency" >> $GITHUB_STEP_SUMMARY

