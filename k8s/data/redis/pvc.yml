apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data
  namespace: data
  labels:
    app: redis
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-premium  # AKS default Premium SSD
  resources:
    requests:
      storage: 10Gi

