apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data
  namespace: data
  labels:
    app: redis
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed  # Standard HDD - Low cost
  resources:
    requests:
      storage: 10Gi

