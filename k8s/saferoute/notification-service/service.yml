apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: saferoute
  labels:
    app: notification-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: notification-service

