apiVersion: v1
kind: ConfigMap
metadata:
  name: user-management-config
  namespace: saferoute
data:
  database.name: "saferoute"
  # Must match JWT iss host (same tenant as mobile/web EXPO_PUBLIC_AUTH0_DOMAIN)
  auth0.domain: "saferouteapp.eu.auth0.com"
  log.level: "info"
  # Redis cache TTL in seconds (default: 3600 = 1 hour)
  redis.cache.ttl: "3600"
  # Auth token cache TTL in seconds (default: 3600 = 1 hour)
  redis.auth.token.ttl: "3600"

