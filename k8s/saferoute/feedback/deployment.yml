apiVersion: apps/v1
kind: Deployment
metadata:
  name: feedback
  namespace: saferoute
  labels:
    app: feedback
    tier: backend
spec:
  replicas: 2
  revisionHistoryLimit: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: feedback
  template:
    metadata:
      labels:
        app: feedback
        tier: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "80"
        prometheus.io/path: "/metrics"
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchLabels:
                  app: feedback
              topologyKey: kubernetes.io/hostname
      containers:
      - name: feedback
        image: saferoute/feedback:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: http
        env:
        - name: POSTGRES_HOST
          value: postgresql.data.svc.cluster.local
        - name: POSTGRES_PORT
          value: "5432"
        - name: POSTGRES_DATABASE
          valueFrom:
            configMapKeyRef:
              name: feedback-config
              key: database.name
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: postgresql-secret
              key: username
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgresql-secret
              key: password
        - name: POSTGRES_SSLMODE
          value: "disable"
        - name: REDIS_HOST
          value: redis.saferoute.svc.cluster.local
        - name: REDIS_PORT
          value: "6379"
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-secret
              key: REDIS_PASSWORD
        - name: RECAPTCHA_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: feedback-recaptcha-secret
              key: RECAPTCHA_SECRET_KEY
        - name: SMTP_HOST
          valueFrom:
            secretKeyRef:
              name: feedback-smtp-secret
              key: SMTP_HOST
        - name: SMTP_PORT
          valueFrom:
            secretKeyRef:
              name: feedback-smtp-secret
              key: SMTP_PORT
        - name: SMTP_USERNAME
          valueFrom:
            secretKeyRef:
              name: feedback-smtp-secret
              key: SMTP_USERNAME
        - name: SMTP_PASSWORD
          valueFrom:
            secretKeyRef:
              name: feedback-smtp-secret
              key: SMTP_PASSWORD
        - name: SYSTEM_FEEDBACK_TO_EMAIL
          valueFrom:
            secretKeyRef:
              name: feedback-smtp-secret
              key: SYSTEM_FEEDBACK_TO_EMAIL
        - name: RABBITMQ_HOST
          value: rabbitmq.data.svc.cluster.local
        - name: RABBITMQ_PORT
          value: "5672"
        - name: RABBITMQ_USER
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secret
              key: username
        - name: RABBITMQ_PASSWORD
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secret
              key: password
        resources:
          requests:
            memory: "64Mi"
            cpu: "25m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5

